# This file intentionally left disabled.
# Real workflow is in docker-publish.yml
name: _disabled
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"